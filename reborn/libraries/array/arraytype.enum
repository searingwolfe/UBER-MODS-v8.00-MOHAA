__init:
	self.MUTABLE = "MUT";		// typeof()->"array" and is not associative
	self.CONSTANT = "CNST";		// typeof()->"const array"
	self.ASSOCIATIVE = "ASSC";	// typeof()->"array" and is associative
	self.ENTITY = "ENT";		// typeof()->"array" and consists of entities with the same targetname
	self.COMPOSITE = "COMP";	// typeof()->"vector", "string" or "const string"
end;